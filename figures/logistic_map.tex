% !TEX TS-program = pdfLaTeX+shellescape
% !TEX encoding = UTF-8 Unicode

\documentclass{standalone}
\usepackage{pgfplots}
\pgfplotsset{compat=1.17}
\usepackage{multido}

\begin{document}
    \begin{tikzpicture}[baseline]
        \begin{axis}[
            scale=0.6, % scale
            xmin=-0.08,xmax=39.92,ymin=-0.08,ymax=1.08, % range of plot
            % legend style={at={(axis cs: 1,0)}, anchor=south east}, % position of legends
            unit vector ratio = {1, 30}, % aspect ratio
            axis lines = middle, % middle or box
            %axis x line = bottom, % top, middle, bottom, none: axis x line* = ... removes arrow heads
            %axis y line = left, % left, center, right, none: axis y line* = ... removes arrow heads
            xlabel = {$k$}, ylabel = {$x_k$}, % axis labels 
        ]
            % \addplot[mark=o] gnuplot [raw gnuplot, mark='o', blue] {
            %     plot 'data_logistic_map1.txt'
            % };
            \addplot[only marks, mark=o, blue] table {data_logistic_map1.txt};
            \addplot[blue, domain=0:40, samples=3]({x}, {1-1/1.2}); 
            \node at (30.5, 1.0) {$a=1.2$};
        \end{axis}
    \end{tikzpicture}
    \begin{tikzpicture}[baseline]
        \begin{axis}[
            scale=0.6, % scale
            xmin=-0.08,xmax=39.92,ymin=-0.08,ymax=1.08, % range of plot
            % legend style={at={(axis cs: 1,0)}, anchor=south east}, % position of legends
            unit vector ratio = {1, 30}, % aspect ratio
            axis lines = middle, % middle or box
            %axis x line = bottom, % top, middle, bottom, none: axis x line* = ... removes arrow heads
            %axis y line = left, % left, center, right, none: axis y line* = ... removes arrow heads
            xlabel = {$k$}, ylabel = {$x_k$}, % axis labels 
        ]
            % \addplot[mark=o] gnuplot [raw gnuplot, mark='o', blue] {
            %     plot 'data_logistic_map1.txt'
            % };
            \addplot[only marks, mark=o, blue] table {data_logistic_map2.txt};
            \addplot[blue, domain=0:40, samples=3]({x}, {1-1/2.8}); 
            \node at (30.5, 1.0) {$a=2.8$};
        \end{axis}
    \end{tikzpicture}
    \begin{tikzpicture}[baseline]
        \begin{axis}[
            scale=0.6, % scale
            xmin=-0.08,xmax=39.92,ymin=-0.08,ymax=1.08, % range of plot
            % legend style={at={(axis cs: 1,0)}, anchor=south east}, % position of legends
            unit vector ratio = {1, 30}, % aspect ratio
            axis lines = middle, % middle or box
            %axis x line = bottom, % top, middle, bottom, none: axis x line* = ... removes arrow heads
            %axis y line = left, % left, center, right, none: axis y line* = ... removes arrow heads
            xlabel = {$k$}, ylabel = {$x_k$}, % axis labels 
        ]
            % \addplot[mark=o] gnuplot [raw gnuplot, mark='o', blue] {
            %     plot 'data_logistic_map1.txt'
            % };
            \addplot[only marks, mark=o, blue] table {data_logistic_map3.txt};
            \addplot[blue, domain=0:40, samples=3]({x}, {1-1/3.6}); 
            \node at (30.5, 1.0) {$a=3.6$};
        \end{axis}
    \end{tikzpicture}
\end{document}